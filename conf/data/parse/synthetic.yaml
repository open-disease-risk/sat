_target_: sat.data.dataset.parse_synthetic.synthetic
source: ${data_source}
processed_dir: ${modelhub}
train_ratio: 0.8
validation_ratio: 0.1
test_ratio: 0.1
n_bins: 20
encode: ordinal
strategy: quantile
name: ${dataset}
kfold: ${cv.kfold}
