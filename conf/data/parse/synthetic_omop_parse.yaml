_target_: sat.data.dataset.parse_omop.omop
source: ${modelhub}/cohort/${dataset}/cohort.parquet
processed_dir: ${modelhub}
scale_method: standard
scale_numerics: true
min_scale_numerics: 1.0
name: ${dataset}
batch_size: 1000
time_field: "time"
primary_key: "patient_id"